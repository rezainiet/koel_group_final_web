/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'placehold.co', 'koelgroupbd.com', 'images.pexels.com'],
    },
};

export default nextConfig;
